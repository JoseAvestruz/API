npm install nodemon --save-dev

nodemon é uma biblioteca parecida com o liveSever basicamente
ela permite que o servdor Atualize ao salvar o arquivo sem a neccessidade de
parar o servidore ligar ele toda que que ouver uma alteracao o --save-dev e uma depedencia ou seja
basicamente diz que ele so sera usado na faze de densevolvimento ou seja so sera utilizado quando
eu estiver a densevolver a aplicacao quando o servdor estiver online ai a gente nao precisa do
nodemon porque ele ja ficara disponivel rodando de forma directa

Resumo da aula 
const express =  require('express')
const app = express()
const port = 3333

app.get('/message/:id/:user',(require,response)=>{
    //route Params Elas sao obrigatorio
    const {id , user} = require.params
    response.send(`Id do usuario e ${id} com o nome ${user}`)
})

//Route Query ja e opcinal  esse  basta acessar a rota
app.get('/user',(request,response)=>{
    const {page , limit, category} = request.query
   // saida  http://localhost:3333/user/?page=2&limit=10
  // http://localhost:3333/user/?page=2&limit=10&category=jogos
    response.send(`Pagina ${page}. Mostrar ${limit} categoria ${category}`);
})

app.listen(port,()=>{ console.log(`Server is Running On Por ${port}`)});

2- Estrutua e organizacao do projecto a pasta routes basicamente armazena todas as rotas que
a gente vai usar. tambem deu se enfase no module.export = "arquivo para ser acessado Global" como 

fosse um link que todos os arquivo que desejam usas suas funcionlidadesuse
tambem faluse do uso do ex: app.json({}) que basicamente diz que o aquivo app.que esta usando o express
queturne da api as informacoes em formato javaScript Objecto Notetion


3- Adicionando biblioteca para tratamentos de erros nessa caso a express async error
npm install express-async-errors --save

